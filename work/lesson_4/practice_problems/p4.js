// Q: What is the return value of the following statement? Why?

['ant', 'bear', 'caterpillar'].pop().length;

// A: It return 11 that is the length of `caterpillar` word.
//    `pop()` extracts the last item from the array, and
//    `length` returns the word's size.