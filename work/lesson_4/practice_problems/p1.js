// Q: What is the return value of the filter method call below? Why?
//
// [1, 2, 3].filter(num => 'hi');

// A: The above code returns `[]` (i.e. empty array).
//    It happens because of the fact there is no `return` statement.